# 回复丢失修复验收（2026-09-24）

基线：`093f5a4a`。范围：原审计 F1–F12 与修复评审发现的相关问题。保留任务开始前已有的 in-flight 标记修复；没有提交、重启应用或部署。

## 当前状态

本次审计 F1–F12 及相关评审发现均已修复。前端全量测试、生产构建、后端定向测试与服务器模式检查通过；store、事件/投影、后端独立复核均无剩余 Critical/Important 发现。交付范围为当前工作区源代码，不包括运行中桌面程序的更新。

## 修复对应关系

| 范围 | 共同边界上的修复 |
| --- | --- |
| F1、F3：完整结论被短副本遮挡、续答误退役 | 共用同轮合并判断：先验证身份/历史对齐归属，再判定内容覆盖；缺失的正文、思考、工具输出保留在本地。移除“必须比上次回读增长”的前提。 |
| F2、F4：旧回读清掉新消息 | 七个详情提交入口携带读取前快照；新 live、已完成本地回复、乐观消息、队列激活或轮次令牌变化均撤销旧读的破坏性覆盖权限。保留显式 fork 清理。 |
| F5：跨帧连续完成漏掉第二轮 | 帧内记录已接受完成事件的 runtime 与 session 所有权，后续完成沿该帧推进；保留错会话和旧停止事件保护。 |
| F6：缺口快照重复或吞掉队列文本 | 恢复快照前先 flush 已接受文本；已完成快照把内容交给本地历史。 |
| F7、F8：关闭抽屉或重挂载丢回复 | 只读视图卸载仅释放自己的订阅/计时器，不删除共享 runtime；已绑定草稿重挂载复用标签保存的 runtime ID。 |
| F9、F10：只读增量投影缺失、消费者互相覆盖 | 复用现有投影 sink；每次注册独立注销，rekey 合并注册；共享投影按连接、消息和序号防重，重叠回放从 canonical 重建。 |
| F11：生产完成路径缺少 transcript 绑定 | internal 完成入口使用捕获的会话身份；完成绑定复用数据库 expected-null 条件更新，不能抢回已被新会话绑定的记录或制造历史副本。 |
| F12：临时恢复失败销毁观察状态 | 保留已接受内容、路由与缓冲事件，使用现有 ingestor 暂停/恢复并重试快照；卸载清理计时器，过期响应检查实例身份。 |

评审还关闭了：解析器/客户端时间戳差异、相同时间戳重发、旧本地副本未退役时的新轮误匹配、正文分块差异导致结论重复、思考先落盘/最终正文后落盘时过早停止补读，以及多订阅者重复追加投影。

按最小修复原则复用现有对齐、块比较、flush、投影重建和数据库条件更新，没有引入新依赖或另一套消息状态机。无法证明覆盖时保留内容，不用超时删除来掩盖问题。

## 最终前端验证

以下在 RR1/RR2 修复完成、生产代码冻结后执行：

- `pnpm test`：退出 0，**623 个套件，10,110 项通过，15 项跳过**（总计 10,125）。
- 原始 store 审计配置：**4/4 通过**；原始 ACP 审计配置：**3/3 通过**。
- `pnpm build`：退出 0，编译、构建类型检查、**33 个静态页面**导出完成。
- 修改的 TypeScript 文件 ESLint：退出 0，无错误；保留 provider 的 **11 条既有 Hook 警告**。Prettier 与 `git diff --check` 通过。
- 独立 store 最终复核：**18 项定向检查通过**；独立事件/投影复核：**16 项通过**；两者均无剩余 Critical/Important 发现。后端源码复核也无剩余此级别发现。
- 独立 TypeScript 全项目检查并非全绿：只读 CompilerHost 基线对照为 **35 → 35 条诊断**，修改/新增文件无新增诊断；既有测试夹具错误仍在。不要将 Next 构建通过等同于独立全项目类型检查通过。

原始复现命令：

```powershell
pnpm exec vitest run --config docs/reviews/reply-loss-audit/vitest.config.ts
pnpm exec vitest run --config docs/reviews/reply-loss-audit/acp-variants.config.ts
```

## Rust 验证

- 最终原子绑定版本：`cargo test --lib --features test-utils acp::lifecycle::tests::internal_completion` **5/5 通过**，覆盖 17 个场景组合；主控在收尾时独立重跑，同样 5/5 通过。
- 后端执行者串行验证：`handle_event_` **20/20 通过**；完成状态单事件测试、通用重绑定保留历史测试各 **1/1 通过**。
- `cargo check --no-default-features --features server --lib`、rustfmt、差异检查通过。默认桌面 feature 已通过上述库测试编译；未构建桌面安装包。
- B1 已验证红→绿：旧写入产生两行并将原行改回旧 session；条件更新后保持一行、新 session 绑定和一次正常 State 事件。
- 主控核对 store 与 Rust 源文件 SHA-256，与独立复核记录一致。B1 用例确定性覆盖数据库写入边界，不声称强制模拟完整 handler 的并发调度。

## 边界与交付

- 这是源代码与自动化验证交付，**运行中的桌面程序尚未更新**；需要使用重新构建的版本才能应用这些修复。没有执行桌面/浏览器端到端操作，也没有全量 Rust suite/clippy。
- 未持久化的块会保留在本地内存，但没有增加进程崩溃后的持久备份。身份有歧义或文本被改写时宁可保留副本，不能保证所有解析器格式均能立即归并。
- 持续快照请求失败仍需后端恢复后才能补齐；数据库绑定错误沿用警告处理，不阻断原有完成状态流程。
- 已修复本次确认的路径，不承诺穷尽未来所有时序。会话 5095 的完整回复仍在 transcript/详情中；没有当时前端快照，不能声称历史事故的唯一原因已被证明。

复核细节：[store](store-final-review.md)、[事件/投影](event-view-rereview.md)、[后端](backend-rereview.md)。实现与红绿测试：[store](store-fix-results.md)、[ACP](acp-fix-results.md)、[视图](views-fix-results.md)、[Rust](backend-fix-results.md)。旧 review/rereview 文档作为修复过程记录保留，当前处置以上述最终复核及本页为准。
