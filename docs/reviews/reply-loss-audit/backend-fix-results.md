# F11 backend completion binding

- Scope: `src-tauri/src/acp/lifecycle.rs` and its inline tests only.
- Root cause confirmed: production `handle_internal_event` routes completion directly to the sidecar-aware status handler, bypassing the legacy missing-`external_id` fallback.
- Regression tests added against the production internal envelope and real SQLite: four representative completion reasons with/without sidecars, captured old identity versus newer live identity or removed connection, already-bound rows, and missing captured session identity.
- RED: `cargo test --lib --features test-utils acp::lifecycle::tests::internal_completion` failed as expected: 2 failed, 2 passed, 7,090 filtered out. Both failures observed actual DB `external_id: None` instead of `captured-session`. Cold build: 7m29s; test execution: 0.62s.
- Implementation: production binding uses the completion sidecar's conversation ID and the event's session ID. Without a sidecar, a conflicting live session prevents binding. Missing captured session IDs are never filled from a newer live session. Legacy and production paths share the existing bind-only behavior and error logging; already-bound rows are preserved.
- GREEN: the same `internal_completion` filter passed all 4 tests (16 scenario combinations); 7,090 filtered out. Rebuild: 6m14s; test execution: 1.96s.
- Legacy compatibility: `cargo test --lib --features test-utils acp::lifecycle::tests::handle_event_` passed all 20 tests, including Grok/Antigravity bindings, cancelled/empty completions, completed-row CAS, and soft-deleted rows; 7,074 filtered out. Test execution: 10.03s.
- `rustfmt --edition 2021 --check src-tauri/src/acp/lifecycle.rs` and scoped `git diff --check` passed.
- `cargo check --no-default-features --features server --lib` passed (1m26s). Desktop/default features compiled successfully in the focused test runs.
- Remaining limits: binding retains the legacy warning-only error policy, so DB binding failures do not prevent status completion. An empty captured session ID or conflicting sidecar-free identity remains unbound rather than borrowing a newer session. No desktop end-to-end run or broad Cargo suite was performed; no commits, application restart, or deployment.
- Final scope: only `src-tauri/src/acp/lifecycle.rs` (production fix plus four inline regressions) and this results document were edited by this worker; unrelated dirty changes were preserved.

## Review follow-up B1: atomic completion binding

- Verified the review finding: the completion fallback read and generic rebind transaction are separate; another connection can bind the row between them. The generic binder can then create an unannounced preserved sibling.
- Confirmed `renormalize_external_id_alias(..., expected_old=None, ...)` already issues one UPDATE guarded by both `external_id IS NULL` and `deleted_at IS NULL`; a competing binding is a silent no-op, and the database unique index remains enforced. No service change is needed.
- Added a real SQLite regression at the existing completion write-helper boundary: observe NULL, commit B with the real generic binder, attempt A's stale completion write, then exercise production `handle_internal_event`. Assertions cover retaining B, exactly one conversation row, consistency with B's live identity, and exactly one normal completion State event with its awaiting-reply token.
- Coverage boundary: this deterministically stages the competing commit before the write helper; it does **not** force scheduler interleaving inside `handle_internal_event`, nor run the event-capture/ingress/FIFO chain. The production null read will be removed rather than instrumented with a test hook. Sidecar-free identity tests establish binding protection only, not a whole-handler status/event no-op.
- RED: `cargo test --lib --features test-utils acp::lifecycle::tests::internal_completion_competing_binding_does_not_split_history -- --exact` failed as intended: actual `(row count=2, C=captured-session)` versus expected `(row count=1, C=newer-session)`. Build: 5m11s; test: 0.16s.
- Replaced the completion-only write helper with the existing expected-null CAS and removed the preliminary `get_by_id` read. The generic session-start/first-link binder and the DB service are unchanged. Existing captured-identity selection, status transitions, and warning-only binding error policy remain intact.
- Renamed the four-reason test to say `representative_stop_reasons`, matching its actual coverage.
- GREEN: `cargo test --lib --features test-utils acp::lifecycle::tests::internal_completion` passed all 5 tests (17 scenario combinations), including B1 retaining one row bound to B and one normal completion State event. Build: 6m47s; tests: 1.93s; 7,090 filtered out.
- Follow-up compatibility: `cargo test --lib --features test-utils acp::lifecycle::tests::handle_event_` passed 20/20 (7.86s); exact `acp::lifecycle::tests::turn_complete_emits_exactly_one_state_event_with_backend_token` passed (0.17s). Legacy status and single-event behavior are retained.
- Exact `db::service::conversation_service::tests::bind_external_id_preserves_the_outgoing_session_on_a_new_row` passed (0.15s), checking that the unchanged general binder still preserves outgoing history when intentionally rebinding.
- `cargo check --no-default-features --features server --lib` passed (1m15s). Scoped rustfmt/diff checks passed. All Cargo commands ran serially; no broad test suite was run.
- B1 outcome: the completion mutation is now one expected-null conditional UPDATE, so a committed competing binding is never repointed or split by completion. The real DB regression went red on the former write helper and green on this CAS implementation; the subsequent production-handler assertions confirm row/live identity consistency and normal State-event count. This is deterministic write-boundary coverage, not a forced scheduler interleaving of the complete handler.
- Remaining limits are unchanged: bind errors are logged without independently retrying; sidecar-free conflicting identity protection is binding-only; no end-to-end ingress/UI test or deployment. This follow-up edits only lifecycle code/tests and this document; no service changes, commits, or subagents.
