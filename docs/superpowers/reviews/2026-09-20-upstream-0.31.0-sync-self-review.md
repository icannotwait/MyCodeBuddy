# Self-review: Upstream 0.31.0 Sync Design + Plan

**Skills used:** using-superpowers → brainstorming (Architectural) → writing-plans → verification-before-completion (gate language).

## Classification

Architectural: multi-subsystem merge (ACP, delegation, FE hydrate, identity, i18n, server). Full design doc + implementation plan required before code.

## Design review

| Item | Status |
| --- | --- |
| YAGNI | No extra product features beyond landing upstream + preserving fork invariants |
| Approaches | A/B/C compared; A (no-ff merge upstream/main) selected |
| Conflict surface | Fresh merge-tree probe: 83 files / 272 blocks; bucketed |
| Fork invariants | Identity, OpenClaw deletion, six-tool delegation, default_enabled=false, post-0.30.7 hydrate fixes listed |
| Incoming capabilities | 0.30.8–0.31.0 summarized with must-keep note |
| Ambiguity | Cargo browser features explicitly MERGE_BOTH with server matrix preserved |
| Placeholder scan | Clean |

## Plan review

| Item | Status |
| --- | --- |
| Spec coverage | Tasks 1–10 cover probe → identity → registry → delegation → ACP/FE → rust leftovers → i18n → commit → verify → PR/main |
| Bite-sizing | Sync merges are coarse by nature; tasks follow resolve order, not fake 2-minute micro-steps for every hunk |
| Verification | Explicit commands; no completion without evidence |
| Main merge | Only after CI/gate green |

## Risks

1. Misaligned conflict hunks in `connection.rs` / FE context (seen in 0.30.7) — mitigated by REWRITE rule.
2. `Cargo.lock` + browser features exploding server builds — mitigated by feature matrix check in Task 9.
3. Tip drift before branch open — Task 1 re-probe hard stop.

## Approval gate

Implementation (Task 1+) must not start until the human partner approves this design + plan.
