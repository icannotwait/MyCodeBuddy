[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
ar = "aarch64-linux-gnu-gcc-ar"

[env]
# libtest threads default to ~2 MiB. Debug-built ACP/delegation futures
# overflow that and abort `cargo test`. 32 MiB is address-space reservation.
RUST_MIN_STACK = "33554432"
