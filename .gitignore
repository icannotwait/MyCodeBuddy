# Logs
/logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
tsconfig.tsbuildinfo

# Next.js
.next/
out/
next-env.d.ts

# Monaco editor local files (copied from node_modules)
public/vs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test artifacts
coverage/
*.snap.new
.vitest-cache/

# macOS signing material. Keep certificates and exported private keys out of git.
*.cer
*.csr
*.p12
certificate-base64.txt
updater-signing.key
updater-signing.key.pub
updater-signing.password
local-build.env
GITHUB_SECRETS.md

# Local linked worktrees
.worktrees/

# Local task/runtime artifacts (WSL disks, unpack trees)
.task-runtimes/
.runclave-unpack/

# Local accelerated tauri build stamps / config override
.codeg-build-cache/

# Agent
.claude
.docs
.superpowers
docs/superpowers

# Tauri sidecar staging (built per-target by prepare-sidecars.mjs).
# The binaries are platform-specific build artifacts; ship them through
# release.yml, not git.
src-tauri/binaries/
# Generated by create_credential_helper_script (embeds absolute binary + data-dir paths).
src-tauri/git-credential-codeg.sh
src-tauri/git-credential-codeg.bat

# pnpm local store (pnpm 11+)
.pnpm-store/
# Generated codex-acp seed (built by src-tauri/scripts/stage-codex-acp.mjs)
src-tauri/resources/codex-acp-seed/
